language: python

python:
    - "3.8"

install:
    - pip install -r requirements.txt
    - pip install coverage

script:
    - coverage run -m unittest discover

after_success:
    - coveralls

notifications:
  email: false
